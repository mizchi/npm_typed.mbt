///|
/// Generated HTML elements for React
/// DO NOT EDIT - This file is auto-generated by gen-jsx

///|
fn[T] common_node(
  tag_name : String,
  props : @core.Any,
  children~ : Array[&@react.ReactNode],
  id~ : String?,
  class~ : String?,
  style~ : Map[String, String]?,
  tab_index~ : Int?,
  ref_~ : @react.ReactRef[T]?,
  key~ : String?,
  on_click~ : @react.MouseEventHandler[T]?,
  on_change~ : @react.ChangeEventHandler[T]?,
  on_keydown~ : @react.KeyboardEventHandler[T]?,
  on_keyup~ : @react.KeyboardEventHandler[T]?,
  on_focus~ : @react.FocusEventHandler[T]?,
  on_blur~ : @react.FocusEventHandler[T]?,
  on_drag~ : @react.DragEventHandler[T]?,
  on_dragstart~ : @react.DragEventHandler[T]?,
  on_dragend~ : @react.DragEventHandler[T]?,
  on_dragleave~ : @react.DragEventHandler[T]?,
  on_dragenter~ : @react.DragEventHandler[T]?,
  on_dragover~ : @react.DragEventHandler[T]?,
  on_drop~ : @react.DragEventHandler[T]?,
) -> @react.Element {
  if id is Some(id) {
    props._set("id", id |> @core.any)
  }
  if class is Some(class) {
    props._set("className", class |> @core.any)
  }
  if tab_index is Some(tab_index) {
    props._set("tabIndex", tab_index |> @core.any)
  }
  if style is Some(style) {
    let style_obj = @core.new_object()
    for k, v in style {
      style_obj._set(k, v |> @core.any)
    }
    props._set("style", style_obj)
  }
  if ref_ is Some(ref_) {
    let ref_js : @core.Any = ref_.as_any().cast()
    props._set("ref", ref_js)
  }
  if on_click is Some(f) {
    props._set("onClick", f |> @js.from_fn1)
  }
  if on_change is Some(f) {
    props._set("onChange", f |> @js.from_fn1)
  }
  if on_keydown is Some(f) {
    props._set("onKeyDown", f |> @js.from_fn1)
  }
  if on_keyup is Some(f) {
    props._set("onKeyUp", f |> @js.from_fn1)
  }
  if on_focus is Some(f) {
    props._set("onFocus", f |> @js.from_fn1)
  }
  if on_blur is Some(f) {
    props._set("onBlur", f |> @js.from_fn1)
  }
  if on_drag is Some(f) {
    props._set("onDrag", f |> @js.from_fn1)
  }
  if on_dragstart is Some(f) {
    props._set("onDragStart", f |> @js.from_fn1)
  }
  if on_dragend is Some(f) {
    props._set("onDragEnd", f |> @js.from_fn1)
  }
  if on_dragleave is Some(f) {
    props._set("onDragLeave", f |> @js.from_fn1)
  }
  if on_dragenter is Some(f) {
    props._set("onDragEnter", f |> @js.from_fn1)
  }
  if on_dragover is Some(f) {
    props._set("onDragOver", f |> @js.from_fn1)
  }
  if on_drop is Some(f) {
    props._set("onDrop", f |> @js.from_fn1)
  }
  let children_count = children.length()
  if children_count > 0 {
    if children_count == 1 {
      props._set("children", children[0].to_react_node())
    } else {
      let children_js : @core.Any = @core.any(children.map(_.to_react_node()))
      props._set("children", children_js)
    }
  }
  let props_js : @core.Any = props.cast()
  @react.createElementWithKey(
    @core.any(tag_name),
    props_js,
    key,
    children_count~,
  )
}

///|
pub fn div(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLDivElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLDivElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLDivElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLDivElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLDivElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLDivElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLDivElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLDivElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLDivElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLDivElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLDivElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLDivElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLDivElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLDivElement],
) -> @react.Element {
  common_node(
    "div",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn span(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLSpanElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLSpanElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLSpanElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLSpanElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLSpanElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLSpanElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLSpanElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLSpanElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLSpanElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLSpanElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLSpanElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLSpanElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLSpanElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLSpanElement],
) -> @react.Element {
  common_node(
    "span",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn p(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLParagraphElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLParagraphElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLParagraphElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLParagraphElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLParagraphElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLParagraphElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLParagraphElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLParagraphElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLParagraphElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLParagraphElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLParagraphElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLParagraphElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLParagraphElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLParagraphElement],
) -> @react.Element {
  common_node(
    "p",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn pre(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "pre",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn code(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "code",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn blockquote(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "blockquote",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn nav(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "nav",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn main_(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "main",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn header(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "header",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn footer(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "footer",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn section(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "section",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn article(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "article",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn aside(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "aside",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn details(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "details",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn summary(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "summary",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn h1(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLHeadingElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLHeadingElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLHeadingElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLHeadingElement],
) -> @react.Element {
  common_node(
    "h1",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn h2(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLHeadingElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLHeadingElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLHeadingElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLHeadingElement],
) -> @react.Element {
  common_node(
    "h2",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn h3(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLHeadingElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLHeadingElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLHeadingElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLHeadingElement],
) -> @react.Element {
  common_node(
    "h3",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn h4(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLHeadingElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLHeadingElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLHeadingElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLHeadingElement],
) -> @react.Element {
  common_node(
    "h4",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn h5(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLHeadingElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLHeadingElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLHeadingElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLHeadingElement],
) -> @react.Element {
  common_node(
    "h5",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn h6(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLHeadingElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLHeadingElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLHeadingElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLHeadingElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLHeadingElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLHeadingElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLHeadingElement],
) -> @react.Element {
  common_node(
    "h6",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn ul(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLUListElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLUListElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLUListElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLUListElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLUListElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLUListElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLUListElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLUListElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLUListElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLUListElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLUListElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLUListElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLUListElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLUListElement],
) -> @react.Element {
  common_node(
    "ul",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn ol(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLOListElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLOListElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLOListElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLOListElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLOListElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLOListElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLOListElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLOListElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLOListElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLOListElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLOListElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLOListElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLOListElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLOListElement],
) -> @react.Element {
  common_node(
    "ol",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn li(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLLIElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLLIElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLLIElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLLIElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLLIElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLLIElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLLIElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLLIElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLLIElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLLIElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLLIElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLLIElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLLIElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLLIElement],
) -> @react.Element {
  common_node(
    "li",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn a(
  children : Array[&@react.ReactNode],
  href? : String,
  target? : String,
  download? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLAnchorElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLAnchorElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLAnchorElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLAnchorElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLAnchorElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLAnchorElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLAnchorElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLAnchorElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLAnchorElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLAnchorElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLAnchorElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLAnchorElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLAnchorElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLAnchorElement],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if href is Some(v) {
    custom_props._set("href", v |> @core.any)
  }
  if target is Some(v) {
    custom_props._set("target", v |> @core.any)
  }
  if download is Some(v) {
    custom_props._set("download", v |> @core.any)
  }
  common_node(
    "a",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn button(
  children : Array[&@react.ReactNode],
  type_? : String,
  disabled? : Bool,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLButtonElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLButtonElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLButtonElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLButtonElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLButtonElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLButtonElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLButtonElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLButtonElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLButtonElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLButtonElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLButtonElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLButtonElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLButtonElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLButtonElement],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if type_ is Some(v) {
    custom_props._set("type", v |> @core.any)
  }
  if disabled is Some(v) {
    custom_props._set("disabled", v |> @core.any)
  }
  common_node(
    "button",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn input(
  type_? : String,
  name? : String,
  value? : String,
  default_value? : String,
  placeholder? : String,
  checked? : Bool,
  disabled? : Bool,
  readonly_? : Bool,
  required? : Bool,
  min? : String,
  max? : String,
  step? : String,
  pattern? : String,
  autocomplete? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLInputElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLInputElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLInputElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLInputElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLInputElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLInputElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLInputElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLInputElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLInputElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLInputElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLInputElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLInputElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLInputElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLInputElement],
  children? : Array[&@react.ReactNode],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if type_ is Some(v) {
    custom_props._set("type", v |> @core.any)
  }
  if name is Some(v) {
    custom_props._set("name", v |> @core.any)
  }
  if value is Some(v) {
    custom_props._set("value", v |> @core.any)
  }
  if default_value is Some(v) {
    custom_props._set("defaultValue", v |> @core.any)
  }
  if placeholder is Some(v) {
    custom_props._set("placeholder", v |> @core.any)
  }
  if checked is Some(v) {
    custom_props._set("checked", v |> @core.any)
  }
  if disabled is Some(v) {
    custom_props._set("disabled", v |> @core.any)
  }
  if readonly_ is Some(v) {
    custom_props._set("readOnly", v |> @core.any)
  }
  if required is Some(v) {
    custom_props._set("required", v |> @core.any)
  }
  if min is Some(v) {
    custom_props._set("min", v |> @core.any)
  }
  if max is Some(v) {
    custom_props._set("max", v |> @core.any)
  }
  if step is Some(v) {
    custom_props._set("step", v |> @core.any)
  }
  if pattern is Some(v) {
    custom_props._set("pattern", v |> @core.any)
  }
  if autocomplete is Some(v) {
    custom_props._set("autoComplete", v |> @core.any)
  }
  common_node(
    "input",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children=children.unwrap_or([]),
  )
}

///|
pub fn textarea(
  children : Array[&@react.ReactNode],
  name? : String,
  value? : String,
  default_value? : String,
  placeholder? : String,
  rows? : Int,
  columns? : Int,
  spellcheck? : Bool,
  disabled? : Bool,
  readonly_? : Bool,
  required? : Bool,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLTextAreaElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLTextAreaElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLTextAreaElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLTextAreaElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLTextAreaElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLTextAreaElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLTextAreaElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLTextAreaElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLTextAreaElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLTextAreaElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLTextAreaElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLTextAreaElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLTextAreaElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLTextAreaElement],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if name is Some(v) {
    custom_props._set("name", v |> @core.any)
  }
  if value is Some(v) {
    custom_props._set("value", v |> @core.any)
  }
  if default_value is Some(v) {
    custom_props._set("defaultValue", v |> @core.any)
  }
  if placeholder is Some(v) {
    custom_props._set("placeholder", v |> @core.any)
  }
  if rows is Some(v) {
    custom_props._set("rows", v |> @core.any)
  }
  if columns is Some(v) {
    custom_props._set("cols", v |> @core.any)
  }
  if spellcheck is Some(v) {
    custom_props._set("spellCheck", v |> @core.any)
  }
  if disabled is Some(v) {
    custom_props._set("disabled", v |> @core.any)
  }
  if readonly_ is Some(v) {
    custom_props._set("readOnly", v |> @core.any)
  }
  if required is Some(v) {
    custom_props._set("required", v |> @core.any)
  }
  common_node(
    "textarea",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn form(
  children : Array[&@react.ReactNode],
  method_? : String,
  action_str? : String,
  enc_type? : String,
  name? : String,
  disabled? : Bool,
  on_submit? : (@dom.FormEvent) -> Unit,
  action? : (@http.FormData) -> Unit,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLFormElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLFormElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLFormElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLFormElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLFormElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLFormElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLFormElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLFormElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLFormElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLFormElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLFormElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLFormElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLFormElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLFormElement],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if method_ is Some(v) {
    custom_props._set("method", v |> @core.any)
  }
  if action_str is Some(v) {
    custom_props._set("action", v |> @core.any)
  }
  if enc_type is Some(v) {
    custom_props._set("encType", v |> @core.any)
  }
  if name is Some(v) {
    custom_props._set("name", v |> @core.any)
  }
  if disabled is Some(v) {
    custom_props._set("disabled", v |> @core.any)
  }
  if on_submit is Some(f) {
    custom_props._set("onSubmit", f |> @js.from_fn1)
  }
  if action is Some(f) {
    custom_props._set("action", f |> @js.from_fn1)
  }
  common_node(
    "form",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn select(
  children : Array[&@react.ReactNode],
  name? : String,
  multiple? : Bool,
  disabled? : Bool,
  required? : Bool,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLSelectElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLSelectElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLSelectElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLSelectElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLSelectElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLSelectElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLSelectElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLSelectElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLSelectElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLSelectElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLSelectElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLSelectElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLSelectElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLSelectElement],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if name is Some(v) {
    custom_props._set("name", v |> @core.any)
  }
  if multiple is Some(v) {
    custom_props._set("multiple", v |> @core.any)
  }
  if disabled is Some(v) {
    custom_props._set("disabled", v |> @core.any)
  }
  if required is Some(v) {
    custom_props._set("required", v |> @core.any)
  }
  common_node(
    "select",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn option(
  children : Array[&@react.ReactNode],
  value~ : String,
  selected? : Bool,
  disabled? : Bool,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLOptionElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLOptionElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLOptionElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLOptionElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLOptionElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLOptionElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLOptionElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLOptionElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLOptionElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLOptionElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLOptionElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLOptionElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLOptionElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLOptionElement],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  custom_props._set("value", value |> @core.any)
  if selected is Some(v) {
    custom_props._set("selected", v |> @core.any)
  }
  if disabled is Some(v) {
    custom_props._set("disabled", v |> @core.any)
  }
  common_node(
    "option",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn label(
  children : Array[&@react.ReactNode],
  htmlFor? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLLabelElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLLabelElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLLabelElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLLabelElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLLabelElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLLabelElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLLabelElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLLabelElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLLabelElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLLabelElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLLabelElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLLabelElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLLabelElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLLabelElement],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if htmlFor is Some(v) {
    custom_props._set("htmlFor", v |> @core.any)
  }
  common_node(
    "label",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn img(
  src~ : String,
  alt? : String,
  width? : Int,
  height? : Int,
  srcset? : String,
  sizes? : String,
  loading? : String,
  crossorigin? : String,
  decoding? : String,
  fetchpriority? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.HTMLImageElement],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.HTMLImageElement],
  on_change? : @react.ChangeEventHandler[@dom.HTMLImageElement],
  on_keydown? : @react.KeyboardEventHandler[@dom.HTMLImageElement],
  on_keyup? : @react.KeyboardEventHandler[@dom.HTMLImageElement],
  on_focus? : @react.FocusEventHandler[@dom.HTMLImageElement],
  on_blur? : @react.FocusEventHandler[@dom.HTMLImageElement],
  on_drag? : @react.DragEventHandler[@dom.HTMLImageElement],
  on_dragstart? : @react.DragEventHandler[@dom.HTMLImageElement],
  on_dragend? : @react.DragEventHandler[@dom.HTMLImageElement],
  on_dragleave? : @react.DragEventHandler[@dom.HTMLImageElement],
  on_dragenter? : @react.DragEventHandler[@dom.HTMLImageElement],
  on_dragover? : @react.DragEventHandler[@dom.HTMLImageElement],
  on_drop? : @react.DragEventHandler[@dom.HTMLImageElement],
  children? : Array[&@react.ReactNode],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  custom_props._set("src", src |> @core.any)
  if alt is Some(v) {
    custom_props._set("alt", v |> @core.any)
  }
  if width is Some(v) {
    custom_props._set("width", v |> @core.any)
  }
  if height is Some(v) {
    custom_props._set("height", v |> @core.any)
  }
  if srcset is Some(v) {
    custom_props._set("srcSet", v |> @core.any)
  }
  if sizes is Some(v) {
    custom_props._set("sizes", v |> @core.any)
  }
  if loading is Some(v) {
    custom_props._set("loading", v |> @core.any)
  }
  if crossorigin is Some(v) {
    custom_props._set("crossOrigin", v |> @core.any)
  }
  if decoding is Some(v) {
    custom_props._set("decoding", v |> @core.any)
  }
  if fetchpriority is Some(v) {
    custom_props._set("fetchPriority", v |> @core.any)
  }
  common_node(
    "img",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children=children.unwrap_or([]),
  )
}

///|
pub fn video(
  children : Array[&@react.ReactNode],
  src? : String,
  controls? : Bool,
  autoplay? : Bool,
  loop_? : Bool,
  muted? : Bool,
  poster? : String,
  width? : Int,
  height? : Int,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if src is Some(v) {
    custom_props._set("src", v |> @core.any)
  }
  if controls is Some(v) {
    custom_props._set("controls", v |> @core.any)
  }
  if autoplay is Some(v) {
    custom_props._set("autoPlay", v |> @core.any)
  }
  if loop_ is Some(v) {
    custom_props._set("loop", v |> @core.any)
  }
  if muted is Some(v) {
    custom_props._set("muted", v |> @core.any)
  }
  if poster is Some(v) {
    custom_props._set("poster", v |> @core.any)
  }
  if width is Some(v) {
    custom_props._set("width", v |> @core.any)
  }
  if height is Some(v) {
    custom_props._set("height", v |> @core.any)
  }
  common_node(
    "video",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn audio(
  children : Array[&@react.ReactNode],
  src? : String,
  controls? : Bool,
  autoplay? : Bool,
  loop_? : Bool,
  muted? : Bool,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if src is Some(v) {
    custom_props._set("src", v |> @core.any)
  }
  if controls is Some(v) {
    custom_props._set("controls", v |> @core.any)
  }
  if autoplay is Some(v) {
    custom_props._set("autoPlay", v |> @core.any)
  }
  if loop_ is Some(v) {
    custom_props._set("loop", v |> @core.any)
  }
  if muted is Some(v) {
    custom_props._set("muted", v |> @core.any)
  }
  common_node(
    "audio",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn source(
  src~ : String,
  type_? : String,
  srcset? : String,
  sizes? : String,
  media? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
  children? : Array[&@react.ReactNode],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  custom_props._set("src", src |> @core.any)
  if type_ is Some(v) {
    custom_props._set("type", v |> @core.any)
  }
  if srcset is Some(v) {
    custom_props._set("srcSet", v |> @core.any)
  }
  if sizes is Some(v) {
    custom_props._set("sizes", v |> @core.any)
  }
  if media is Some(v) {
    custom_props._set("media", v |> @core.any)
  }
  common_node(
    "source",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children=children.unwrap_or([]),
  )
}

///|
pub fn iframe(
  children : Array[&@react.ReactNode],
  src~ : String,
  width? : Int,
  height? : Int,
  sandbox? : String,
  allowfullscreen? : Bool,
  loading? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  custom_props._set("src", src |> @core.any)
  if width is Some(v) {
    custom_props._set("width", v |> @core.any)
  }
  if height is Some(v) {
    custom_props._set("height", v |> @core.any)
  }
  if sandbox is Some(v) {
    custom_props._set("sandbox", v |> @core.any)
  }
  if allowfullscreen is Some(v) {
    custom_props._set("allowFullScreen", v |> @core.any)
  }
  if loading is Some(v) {
    custom_props._set("loading", v |> @core.any)
  }
  common_node(
    "iframe",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn hr(
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
  children? : Array[&@react.ReactNode],
) -> @react.Element {
  common_node(
    "hr",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children=children.unwrap_or([]),
  )
}

///|
pub fn br(
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
  children? : Array[&@react.ReactNode],
) -> @react.Element {
  common_node(
    "br",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children=children.unwrap_or([]),
  )
}

///|
pub fn table(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "table",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn thead(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "thead",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn tbody(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "tbody",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn tfoot(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "tfoot",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn tr(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "tr",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn th(
  children : Array[&@react.ReactNode],
  colspan? : Int,
  rowspan? : Int,
  scope? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if colspan is Some(v) {
    custom_props._set("colSpan", v |> @core.any)
  }
  if rowspan is Some(v) {
    custom_props._set("rowSpan", v |> @core.any)
  }
  if scope is Some(v) {
    custom_props._set("scope", v |> @core.any)
  }
  common_node(
    "th",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn td(
  children : Array[&@react.ReactNode],
  colspan? : Int,
  rowspan? : Int,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if colspan is Some(v) {
    custom_props._set("colSpan", v |> @core.any)
  }
  if rowspan is Some(v) {
    custom_props._set("rowSpan", v |> @core.any)
  }
  common_node(
    "td",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn canvas(
  children : Array[&@react.ReactNode],
  width? : Int,
  height? : Int,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if width is Some(v) {
    custom_props._set("width", v |> @core.any)
  }
  if height is Some(v) {
    custom_props._set("height", v |> @core.any)
  }
  common_node(
    "canvas",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn progress(
  children : Array[&@react.ReactNode],
  value? : Double,
  max? : Double,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if value is Some(v) {
    custom_props._set("value", v |> @core.any)
  }
  if max is Some(v) {
    custom_props._set("max", v |> @core.any)
  }
  common_node(
    "progress",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn meter(
  children : Array[&@react.ReactNode],
  value? : Double,
  min? : Double,
  max? : Double,
  low? : Double,
  high? : Double,
  optimum? : Double,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if value is Some(v) {
    custom_props._set("value", v |> @core.any)
  }
  if min is Some(v) {
    custom_props._set("min", v |> @core.any)
  }
  if max is Some(v) {
    custom_props._set("max", v |> @core.any)
  }
  if low is Some(v) {
    custom_props._set("low", v |> @core.any)
  }
  if high is Some(v) {
    custom_props._set("high", v |> @core.any)
  }
  if optimum is Some(v) {
    custom_props._set("optimum", v |> @core.any)
  }
  common_node(
    "meter",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn dialog(
  children : Array[&@react.ReactNode],
  open_? : Bool,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if open_ is Some(v) {
    custom_props._set("open", v |> @core.any)
  }
  common_node(
    "dialog",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn strong(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "strong",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn em(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "em",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn small(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "small",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn mark(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "mark",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn del(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "del",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn ins(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "ins",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn sub(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "sub",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn sup(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "sup",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn abbr(
  children : Array[&@react.ReactNode],
  title? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if title is Some(v) {
    custom_props._set("title", v |> @core.any)
  }
  common_node(
    "abbr",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn time_(
  children : Array[&@react.ReactNode],
  datetime? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if datetime is Some(v) {
    custom_props._set("dateTime", v |> @core.any)
  }
  common_node(
    "time",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn data_(
  children : Array[&@react.ReactNode],
  value? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if value is Some(v) {
    custom_props._set("value", v |> @core.any)
  }
  common_node(
    "data",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn fieldset(
  children : Array[&@react.ReactNode],
  disabled? : Bool,
  name? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if disabled is Some(v) {
    custom_props._set("disabled", v |> @core.any)
  }
  if name is Some(v) {
    custom_props._set("name", v |> @core.any)
  }
  common_node(
    "fieldset",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn legend(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "legend",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn datalist(
  children : Array[&@react.ReactNode],
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  common_node(
    "datalist",
    match props {
      Some(p) => p
      None => @core.new_object()
    },
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}

///|
pub fn output(
  children : Array[&@react.ReactNode],
  for_? : String,
  name? : String,
  props? : @core.Any,
  id? : String,
  class? : String,
  style? : Map[String, String],
  tab_index? : Int,
  ref_? : @react.ReactRef[@dom.Element],
  key? : String,
  on_click? : @react.MouseEventHandler[@dom.Element],
  on_change? : @react.ChangeEventHandler[@dom.Element],
  on_keydown? : @react.KeyboardEventHandler[@dom.Element],
  on_keyup? : @react.KeyboardEventHandler[@dom.Element],
  on_focus? : @react.FocusEventHandler[@dom.Element],
  on_blur? : @react.FocusEventHandler[@dom.Element],
  on_drag? : @react.DragEventHandler[@dom.Element],
  on_dragstart? : @react.DragEventHandler[@dom.Element],
  on_dragend? : @react.DragEventHandler[@dom.Element],
  on_dragleave? : @react.DragEventHandler[@dom.Element],
  on_dragenter? : @react.DragEventHandler[@dom.Element],
  on_dragover? : @react.DragEventHandler[@dom.Element],
  on_drop? : @react.DragEventHandler[@dom.Element],
) -> @react.Element {
  let custom_props = match props {
    Some(p) => @core.object_assign(@core.new_object(), p)
    None => @core.new_object()
  }
  if for_ is Some(v) {
    custom_props._set("htmlFor", v |> @core.any)
  }
  if name is Some(v) {
    custom_props._set("name", v |> @core.any)
  }
  common_node(
    "output",
    custom_props,
    id~,
    class~,
    style~,
    tab_index~,
    ref_~,
    key~,
    on_click~,
    on_change~,
    on_keydown~,
    on_keyup~,
    on_focus~,
    on_blur~,
    on_drag~,
    on_dragstart~,
    on_dragend~,
    on_dragleave~,
    on_dragenter~,
    on_dragover~,
    on_drop~,
    children~,
  )
}
