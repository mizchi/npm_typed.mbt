React example with react-router

## How to use

```bash
# root
pnpm install

# Most user does not need build.
# specialize output for this example dir
moon build . --sarget-dir target # emit target/*
pnpm vite dev
```
