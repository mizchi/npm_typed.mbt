{
  "name": "@examples/router_app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "moon build . --target-dir target && vite dev",
    "build": "moon build . --target-dir target && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0-beta.0"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router": "^7.0.0"
  }
}