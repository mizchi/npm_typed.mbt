///|
/// Generated HTML elements for Preact
/// DO NOT EDIT - This file is auto-generated by gen-jsx

///|
pub fn div(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "div",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn span(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "span",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn p(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "p",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn pre(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "pre",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn code(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "code",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn blockquote(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "blockquote",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn nav(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "nav",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn main_(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "main",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn header(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "header",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn footer(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "footer",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn section(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "section",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn article(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "article",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn aside(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "aside",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn details(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "details",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn summary(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "summary",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn h1(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "h1",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn h2(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "h2",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn h3(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "h3",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn h4(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "h4",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn h5(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "h5",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn h6(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "h6",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn ul(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "ul",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn ol(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "ol",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn li(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "li",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn a(
  children : Array[&@preact.PreactNode],
  href? : String,
  target? : String,
  download? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if href is Some(v) {
    custom_props["href"] = v |> @core.any
  }
  if target is Some(v) {
    custom_props["target"] = v |> @core.any
  }
  if download is Some(v) {
    custom_props["download"] = v |> @core.any
  }
  @preact.h(
    "a",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn button(
  children : Array[&@preact.PreactNode],
  type_? : String,
  disabled? : Bool,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if type_ is Some(v) {
    custom_props["type"] = v |> @core.any
  }
  if disabled is Some(v) {
    custom_props["disabled"] = v |> @core.any
  }
  @preact.h(
    "button",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn input(
  type_? : String,
  name? : String,
  value? : String,
  default_value? : String,
  placeholder? : String,
  checked? : Bool,
  disabled? : Bool,
  readonly_? : Bool,
  required? : Bool,
  min? : String,
  max? : String,
  step? : String,
  pattern? : String,
  autocomplete? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
  children? : Array[&@preact.PreactNode],
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if type_ is Some(v) {
    custom_props["type"] = v |> @core.any
  }
  if name is Some(v) {
    custom_props["name"] = v |> @core.any
  }
  if value is Some(v) {
    custom_props["value"] = v |> @core.any
  }
  if default_value is Some(v) {
    custom_props["defaultValue"] = v |> @core.any
  }
  if placeholder is Some(v) {
    custom_props["placeholder"] = v |> @core.any
  }
  if checked is Some(v) {
    custom_props["checked"] = v |> @core.any
  }
  if disabled is Some(v) {
    custom_props["disabled"] = v |> @core.any
  }
  if readonly_ is Some(v) {
    custom_props["readOnly"] = v |> @core.any
  }
  if required is Some(v) {
    custom_props["required"] = v |> @core.any
  }
  if min is Some(v) {
    custom_props["min"] = v |> @core.any
  }
  if max is Some(v) {
    custom_props["max"] = v |> @core.any
  }
  if step is Some(v) {
    custom_props["step"] = v |> @core.any
  }
  if pattern is Some(v) {
    custom_props["pattern"] = v |> @core.any
  }
  if autocomplete is Some(v) {
    custom_props["autoComplete"] = v |> @core.any
  }
  @preact.h(
    "input",
    children.unwrap_or([]),
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn textarea(
  children : Array[&@preact.PreactNode],
  name? : String,
  value? : String,
  default_value? : String,
  placeholder? : String,
  rows? : Int,
  columns? : Int,
  spellcheck? : Bool,
  disabled? : Bool,
  readonly_? : Bool,
  required? : Bool,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if name is Some(v) {
    custom_props["name"] = v |> @core.any
  }
  if value is Some(v) {
    custom_props["value"] = v |> @core.any
  }
  if default_value is Some(v) {
    custom_props["defaultValue"] = v |> @core.any
  }
  if placeholder is Some(v) {
    custom_props["placeholder"] = v |> @core.any
  }
  if rows is Some(v) {
    custom_props["rows"] = v |> @core.any
  }
  if columns is Some(v) {
    custom_props["cols"] = v |> @core.any
  }
  if spellcheck is Some(v) {
    custom_props["spellCheck"] = v |> @core.any
  }
  if disabled is Some(v) {
    custom_props["disabled"] = v |> @core.any
  }
  if readonly_ is Some(v) {
    custom_props["readOnly"] = v |> @core.any
  }
  if required is Some(v) {
    custom_props["required"] = v |> @core.any
  }
  @preact.h(
    "textarea",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn form(
  children : Array[&@preact.PreactNode],
  method_? : String,
  action_str? : String,
  enc_type? : String,
  name? : String,
  disabled? : Bool,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if method_ is Some(v) {
    custom_props["method"] = v |> @core.any
  }
  if action_str is Some(v) {
    custom_props["action"] = v |> @core.any
  }
  if enc_type is Some(v) {
    custom_props["encType"] = v |> @core.any
  }
  if name is Some(v) {
    custom_props["name"] = v |> @core.any
  }
  if disabled is Some(v) {
    custom_props["disabled"] = v |> @core.any
  }
  @preact.h(
    "form",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn select(
  children : Array[&@preact.PreactNode],
  name? : String,
  multiple? : Bool,
  disabled? : Bool,
  required? : Bool,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if name is Some(v) {
    custom_props["name"] = v |> @core.any
  }
  if multiple is Some(v) {
    custom_props["multiple"] = v |> @core.any
  }
  if disabled is Some(v) {
    custom_props["disabled"] = v |> @core.any
  }
  if required is Some(v) {
    custom_props["required"] = v |> @core.any
  }
  @preact.h(
    "select",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn option(
  children : Array[&@preact.PreactNode],
  value~ : String,
  selected? : Bool,
  disabled? : Bool,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  custom_props["value"] = value |> @core.any
  if selected is Some(v) {
    custom_props["selected"] = v |> @core.any
  }
  if disabled is Some(v) {
    custom_props["disabled"] = v |> @core.any
  }
  @preact.h(
    "option",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn label(
  children : Array[&@preact.PreactNode],
  htmlFor? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if htmlFor is Some(v) {
    custom_props["htmlFor"] = v |> @core.any
  }
  @preact.h(
    "label",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn img(
  src~ : String,
  alt? : String,
  width? : Int,
  height? : Int,
  srcset? : String,
  sizes? : String,
  loading? : String,
  crossorigin? : String,
  decoding? : String,
  fetchpriority? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
  children? : Array[&@preact.PreactNode],
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  custom_props["src"] = src |> @core.any
  if alt is Some(v) {
    custom_props["alt"] = v |> @core.any
  }
  if width is Some(v) {
    custom_props["width"] = v |> @core.any
  }
  if height is Some(v) {
    custom_props["height"] = v |> @core.any
  }
  if srcset is Some(v) {
    custom_props["srcSet"] = v |> @core.any
  }
  if sizes is Some(v) {
    custom_props["sizes"] = v |> @core.any
  }
  if loading is Some(v) {
    custom_props["loading"] = v |> @core.any
  }
  if crossorigin is Some(v) {
    custom_props["crossOrigin"] = v |> @core.any
  }
  if decoding is Some(v) {
    custom_props["decoding"] = v |> @core.any
  }
  if fetchpriority is Some(v) {
    custom_props["fetchPriority"] = v |> @core.any
  }
  @preact.h(
    "img",
    children.unwrap_or([]),
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn video(
  children : Array[&@preact.PreactNode],
  src? : String,
  controls? : Bool,
  autoplay? : Bool,
  loop_? : Bool,
  muted? : Bool,
  poster? : String,
  width? : Int,
  height? : Int,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if src is Some(v) {
    custom_props["src"] = v |> @core.any
  }
  if controls is Some(v) {
    custom_props["controls"] = v |> @core.any
  }
  if autoplay is Some(v) {
    custom_props["autoPlay"] = v |> @core.any
  }
  if loop_ is Some(v) {
    custom_props["loop"] = v |> @core.any
  }
  if muted is Some(v) {
    custom_props["muted"] = v |> @core.any
  }
  if poster is Some(v) {
    custom_props["poster"] = v |> @core.any
  }
  if width is Some(v) {
    custom_props["width"] = v |> @core.any
  }
  if height is Some(v) {
    custom_props["height"] = v |> @core.any
  }
  @preact.h(
    "video",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn audio(
  children : Array[&@preact.PreactNode],
  src? : String,
  controls? : Bool,
  autoplay? : Bool,
  loop_? : Bool,
  muted? : Bool,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if src is Some(v) {
    custom_props["src"] = v |> @core.any
  }
  if controls is Some(v) {
    custom_props["controls"] = v |> @core.any
  }
  if autoplay is Some(v) {
    custom_props["autoPlay"] = v |> @core.any
  }
  if loop_ is Some(v) {
    custom_props["loop"] = v |> @core.any
  }
  if muted is Some(v) {
    custom_props["muted"] = v |> @core.any
  }
  @preact.h(
    "audio",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn source(
  src~ : String,
  type_? : String,
  srcset? : String,
  sizes? : String,
  media? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
  children? : Array[&@preact.PreactNode],
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  custom_props["src"] = src |> @core.any
  if type_ is Some(v) {
    custom_props["type"] = v |> @core.any
  }
  if srcset is Some(v) {
    custom_props["srcSet"] = v |> @core.any
  }
  if sizes is Some(v) {
    custom_props["sizes"] = v |> @core.any
  }
  if media is Some(v) {
    custom_props["media"] = v |> @core.any
  }
  @preact.h(
    "source",
    children.unwrap_or([]),
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn iframe(
  children : Array[&@preact.PreactNode],
  src~ : String,
  width? : Int,
  height? : Int,
  sandbox? : String,
  allowfullscreen? : Bool,
  loading? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  custom_props["src"] = src |> @core.any
  if width is Some(v) {
    custom_props["width"] = v |> @core.any
  }
  if height is Some(v) {
    custom_props["height"] = v |> @core.any
  }
  if sandbox is Some(v) {
    custom_props["sandbox"] = v |> @core.any
  }
  if allowfullscreen is Some(v) {
    custom_props["allowFullScreen"] = v |> @core.any
  }
  if loading is Some(v) {
    custom_props["loading"] = v |> @core.any
  }
  @preact.h(
    "iframe",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn hr(
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
  children? : Array[&@preact.PreactNode],
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "hr",
    children.unwrap_or([]),
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn br(
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
  children? : Array[&@preact.PreactNode],
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "br",
    children.unwrap_or([]),
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn table(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "table",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn thead(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "thead",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn tbody(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "tbody",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn tfoot(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "tfoot",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn tr(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "tr",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn th(
  children : Array[&@preact.PreactNode],
  colspan? : Int,
  rowspan? : Int,
  scope? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if colspan is Some(v) {
    custom_props["colSpan"] = v |> @core.any
  }
  if rowspan is Some(v) {
    custom_props["rowSpan"] = v |> @core.any
  }
  if scope is Some(v) {
    custom_props["scope"] = v |> @core.any
  }
  @preact.h(
    "th",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn td(
  children : Array[&@preact.PreactNode],
  colspan? : Int,
  rowspan? : Int,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if colspan is Some(v) {
    custom_props["colSpan"] = v |> @core.any
  }
  if rowspan is Some(v) {
    custom_props["rowSpan"] = v |> @core.any
  }
  @preact.h(
    "td",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn canvas(
  children : Array[&@preact.PreactNode],
  width? : Int,
  height? : Int,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if width is Some(v) {
    custom_props["width"] = v |> @core.any
  }
  if height is Some(v) {
    custom_props["height"] = v |> @core.any
  }
  @preact.h(
    "canvas",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn progress(
  children : Array[&@preact.PreactNode],
  value? : Double,
  max? : Double,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if value is Some(v) {
    custom_props["value"] = v |> @core.any
  }
  if max is Some(v) {
    custom_props["max"] = v |> @core.any
  }
  @preact.h(
    "progress",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn meter(
  children : Array[&@preact.PreactNode],
  value? : Double,
  min? : Double,
  max? : Double,
  low? : Double,
  high? : Double,
  optimum? : Double,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if value is Some(v) {
    custom_props["value"] = v |> @core.any
  }
  if min is Some(v) {
    custom_props["min"] = v |> @core.any
  }
  if max is Some(v) {
    custom_props["max"] = v |> @core.any
  }
  if low is Some(v) {
    custom_props["low"] = v |> @core.any
  }
  if high is Some(v) {
    custom_props["high"] = v |> @core.any
  }
  if optimum is Some(v) {
    custom_props["optimum"] = v |> @core.any
  }
  @preact.h(
    "meter",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn dialog(
  children : Array[&@preact.PreactNode],
  open_? : Bool,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if open_ is Some(v) {
    custom_props["open"] = v |> @core.any
  }
  @preact.h(
    "dialog",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn strong(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "strong",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn em(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "em",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn small(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "small",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn mark(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "mark",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn del(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "del",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn ins(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "ins",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn sub(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "sub",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn sup(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "sup",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn abbr(
  children : Array[&@preact.PreactNode],
  title? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if title is Some(v) {
    custom_props["title"] = v |> @core.any
  }
  @preact.h(
    "abbr",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn time_(
  children : Array[&@preact.PreactNode],
  datetime? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if datetime is Some(v) {
    custom_props["dateTime"] = v |> @core.any
  }
  @preact.h(
    "time",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn data_(
  children : Array[&@preact.PreactNode],
  value? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if value is Some(v) {
    custom_props["value"] = v |> @core.any
  }
  @preact.h(
    "data",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn fieldset(
  children : Array[&@preact.PreactNode],
  disabled? : Bool,
  name? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if disabled is Some(v) {
    custom_props["disabled"] = v |> @core.any
  }
  if name is Some(v) {
    custom_props["name"] = v |> @core.any
  }
  @preact.h(
    "fieldset",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn legend(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "legend",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn datalist(
  children : Array[&@preact.PreactNode],
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  @preact.h(
    "datalist",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}

///|
pub fn output(
  children : Array[&@preact.PreactNode],
  for_? : String,
  name? : String,
  props? : Map[String, @core.Any],
  id? : String,
  style? : Map[String, String],
  ref_? : @preact.Ref[@dom.Element],
  key? : String,
) -> @preact.VNode {
  let custom_props : Map[String, @core.Any] = match props {
    Some(p) => p
    None => {}
  }
  if id is Some(id) {
    custom_props["id"] = id |> @core.any
  }
  if for_ is Some(v) {
    custom_props["htmlFor"] = v |> @core.any
  }
  if name is Some(v) {
    custom_props["name"] = v |> @core.any
  }
  @preact.h(
    "output",
    children,
    props=custom_props,
    style?,
    ref_?,
    key?,
  )
}
